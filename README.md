# Quartu Site

A Python project with fixed bugs and AI-assisted corrections.

## Files

- `Untitled-1.py` - Fixed Python script with all bugs resolved

## Fixed Bugs

1. Fixed assignment operator in `calculate_average()` - changed `=` to `+=`
2. Fixed variable name typo in `greet()` - `mesage` to `message`
3. Added missing colon after `if` statement in `find_max()`
4. Added missing colon after `for` loop in `reverse_string()`
5. Fixed comparison operator in `check_even()` - `=` to `==`

## Running the Script

```bash
python3 Untitled-1.py
```

Expected output:
```
3.0
Hello, Alice
5
olleh
True
```
